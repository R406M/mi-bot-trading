Flask>=2.0.0
gunicorn>=20.0.0
kucoin-python>=1.0.0
requests>=2.0.0
python-dotenv>=0.20.0  # Opcional, para manejo de .env localmente
flask-cors>=3.0.0      # Opcional, para manejar CORS si es necesario
